# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Embedding mode: "local" or "gemini"
# local = Use local CLIP model (default, privacy-preserving)
# gemini = Use Google Gemini Embedding API (requires internet, faster)
EMBEDDING_MODE=local


